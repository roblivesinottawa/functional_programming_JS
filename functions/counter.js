// implement a function to receive an integer value and return the value increased by 1

let counter = 1;

const increaseCounter = (value) => value + 1;
console.log(increaseCounter(counter));
// console.log(counter);
